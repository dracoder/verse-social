{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Portfolio repository - dependencies not included' && exit 1",
    "build": "echo 'Portfolio repository - dependencies not included' && exit 1",
    "preview": "echo 'Portfolio repository - dependencies not included' && exit 1"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "laravel-echo": "^2.0.2",
    "pusher-js": "^8.4.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vuex": "^4.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.0"
  }
}
