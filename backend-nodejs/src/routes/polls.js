const express = require('express');
const router = express.Router();

// Placeholder for polls routes
router.get('/', (req, res) => {
  res.json({ message: 'Polls routes - coming soon' });
});

module.exports = router; 